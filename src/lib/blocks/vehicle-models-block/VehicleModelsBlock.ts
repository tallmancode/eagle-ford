import type { Block } from 'payload'

export const VehicleModelsBlock: Block = {
  slug: 'vehicle-models',
  interfaceName: 'VehicleModelsBlock',
  labels: {
    singular: 'Vehicle Models',
    plural: 'Vehicle Models',
  },
  admin: {
    group: 'Vehicle',
  },
  fields: [],
}
