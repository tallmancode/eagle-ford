# Agent skills (this brand)

## Payload CMS

**Canonical skill = Eagle workspace root only** — not this brand repo.

| How you opened Cursor                           | Read this path                                                       |
| ----------------------------------------------- | -------------------------------------------------------------------- |
| Multi-root `eagle-motor-company.code-workspace` | workspace `.agents/skills/payload/SKILL.md`                          |
| From this brand repo root                       | `../.agents/skills/payload/SKILL.md`                                 |
| Absolute (typical Windows layout)               | `C:\development\Eagle Motor Company\.agents\skills\payload\SKILL.md` |

Reference docs: `../.agents/skills/payload/reference/` (from brand root).

Any local `payload/` folder here is a **pointer / legacy archive**, not the skill to follow. Do **not** add git-tracked junctions or symlinks into the workspace skill (brand git would see workspace files as local modifications).

Brand-only Cursor skills (if any) live under `.cursor/skills/`.
