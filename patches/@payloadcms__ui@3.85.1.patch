diff --git a/dist/forms/Form/reduceToSerializableFields.d.ts b/dist/forms/Form/reduceToSerializableFields.d.ts
index 59c4c191a4123358bb13afc8d831a1ac4d07c655..4e4e77520c744a549db140d88781808a6e71fd9a 100644
--- a/dist/forms/Form/reduceToSerializableFields.d.ts
+++ b/dist/forms/Form/reduceToSerializableFields.d.ts
@@ -1,11 +1,19 @@
-import { type FormField, type FormState } from 'payload';
-type BlacklistedKeys = 'customComponents' | 'validate';
+import type { FormState, FormStateWithoutComponents } from 'payload';
 /**
- * Takes in FormState and removes fields that are not serializable.
- * Returns FormState without blacklisted keys.
+ * Takes in FormState and returns a JSON-serializable copy of it.
+ *
+ * Form state carries React nodes at two depths — `field.customComponents` and
+ * `field.rows[i].customComponents` — and a rendered component (e.g. a `RowLabel`
+ * for a populated relationship row) can hold references back into the running
+ * Payload instance, which makes the state impossible to `JSON.stringify`.
+ *
+ * This defers to `deepCopyObjectSimpleWithoutReactComponents` — the same helper
+ * `Form/index.tsx` uses before sending form state to the server — which prunes
+ * every React element by its `$$typeof` symbol at any depth (so both the
+ * field-level and row-level component trees are removed), preserves the plain
+ * data (values, row metadata, filter options, dates, ObjectIds), and drops
+ * `File` values that cannot be serialized.
  */
-export declare const reduceToSerializableFields: (fields: FormState) => {
-    [key: string]: Omit<FormField, BlacklistedKeys>;
-};
+export declare const reduceToSerializableFields: (fields: FormState) => FormStateWithoutComponents;
 export {};
 //# sourceMappingURL=reduceToSerializableFields.d.ts.map
diff --git a/dist/forms/Form/reduceToSerializableFields.js b/dist/forms/Form/reduceToSerializableFields.js
index 5a564d4bbb37a8e060733c1d4eec61d84b0e0de7..3af6c1cdf9831cc290cfec9ced28573d90e121b3 100644
--- a/dist/forms/Form/reduceToSerializableFields.js
+++ b/dist/forms/Form/reduceToSerializableFields.js
@@ -1,23 +1,24 @@
-const blacklistedKeys = ['validate', 'customComponents'];
-const sanitizeField = incomingField => {
-  const field = {
-    ...incomingField
-  } // shallow copy, as we only need to remove top-level keys
-;
-  for (const key of blacklistedKeys) {
-    delete field[key];
-  }
-  return field;
-};
+import { deepCopyObjectSimpleWithoutReactComponents } from 'payload/shared';
 /**
- * Takes in FormState and removes fields that are not serializable.
- * Returns FormState without blacklisted keys.
+ * Takes in FormState and returns a JSON-serializable copy of it.
+ *
+ * Form state carries React nodes at two depths — `field.customComponents` and
+ * `field.rows[i].customComponents` — and a rendered component (e.g. a `RowLabel`
+ * for a populated relationship row) can hold references back into the running
+ * Payload instance, which makes the state impossible to `JSON.stringify`.
+ *
+ * This defers to `deepCopyObjectSimpleWithoutReactComponents` — the same helper
+ * `Form/index.tsx` uses before sending form state to the server — which prunes
+ * every React element by its `$$typeof` symbol at any depth (so both the
+ * field-level and row-level component trees are removed), preserves the plain
+ * data (values, row metadata, filter options, dates, ObjectIds), and drops
+ * `File` values that cannot be serialized.
+ *
+ * @see https://github.com/payloadcms/payload/issues/16786
+ * @see https://github.com/payloadcms/payload/pull/17427
  */
-export const reduceToSerializableFields = fields => {
-  const result = {};
-  for (const key in fields) {
-    result[key] = sanitizeField(fields[key]);
-  }
-  return result;
-};
+export const reduceToSerializableFields = fields =>
+  deepCopyObjectSimpleWithoutReactComponents(fields, {
+    excludeFiles: true
+  });
 //# sourceMappingURL=reduceToSerializableFields.js.map
